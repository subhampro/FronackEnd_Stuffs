const plus = (x,y) => x+y;
const minus = (x,y) => x-y;
const gunah = (x,y) => x*y;
const vag = (x,y) => x/y;
const pi = 3.14159;

module.exports.pi = pi;
module.exports.vag = vag;
module.exports.gunah = gunah;
module.exports.minus = minus;
module.exports.plus = plus;
// module.exports = { plus, minus, gunah, vag, pi };
